import { SnowdenProcessor } from '../modules/SnowdenProcessor';
import { expect } from 'chai';
import 'mocha';

describe('Snowden', () => {

  /*let policyNumber = '200311000001'

  it('Calling snowden without policynumber', () => {
    const result = new SnowdenProcessor().execute();    
  });

  it('Calling snowden with policynumber', () => {
    const result = new SnowdenProcessor().execute(policyNumber);    
  });*/

});